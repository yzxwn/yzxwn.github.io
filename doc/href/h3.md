## 资料3

### js笔记

1.数据类型
```
判断数组：Array.isArray(arr)
typeof:未定义-undefined，对象|null|数组-object
判断对象和函数，一个变量是否是某个对象的实例：new Array() instanceof Array === true
Object.prototype.toString.call(undefined) === "[object Undefined]" //准确返回首字母大写数据类型
```
